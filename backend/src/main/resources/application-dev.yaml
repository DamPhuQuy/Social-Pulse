spring:
  datasource:
    url: jdbc:${DB:postgresql}://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:postgres}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: ${DB_DRIVER:org.postgresql.Driver}
  flyway:
    enabled: true
    locations: classpath:db/migration
    clean-disabled: false
  jpa:
    database: ${DB:postgresql}
    hibernate:
      ddl-auto: validate
    show-sql: true
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL_USERNAME}
    password: ${EMAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  jwt:
    secret: ${JWT_SECRET:dev-only-super-secret-key-for-socialpulse-2024}
    expiration-ms: ${JWT_EXPIRATION_MS:900000}
    refresh-expiration-ms: ${JWT_REFRESH_EXPIRATION_MS:604800000}
  version: 1.0.0

ai:
  lightgbm:
    enabled: ${AI_LIGHTGBM_ENABLED:false}
    model-location: ${AI_LIGHTGBM_MODEL_LOCATION:classpath:ai/lightgbm-ranking-model.json}
    feature-schema-version: ${AI_LIGHTGBM_FEATURE_SCHEMA_VERSION:v1}
  service:
    enabled: ${AI_SERVICE_ENABLED:false}
    url: ${AI_SERVICE_URL:http://localhost:5000}
    timeout-ms: ${AI_SERVICE_TIMEOUT_MS:1500}
    ranking-path: ${AI_SERVICE_RANKING_PATH:/rank}

cloudinary:
  url: ${CLOUDINARY_URL:}
  cloud-name: ${CLOUDINARY_CLOUD_NAME:}
  api-key: ${CLOUDINARY_API_KEY:}
  api-secret: ${CLOUDINARY_API_SECRET:}
