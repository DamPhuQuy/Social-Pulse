[project]
name = "ai-pipeline"
version = "0.1.0"
description = "Social Pulse AI Pipeline - LightGBM feed ranking"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.26.0",
    "zstandard>=0.22.0",
]

[project.scripts]
train = "ai_pipeline.training.cli:main"
